#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/protocol/caldav/version"

version = Protocol::Caldav::VERSION
tag = "protocol-caldav-v#{version}"

existing = `git tag -l #{tag}`.strip
unless existing.empty?
  abort "ERROR: Tag #{tag} already exists"
end

system("git tag #{tag}") || abort("ERROR: Failed to create tag #{tag}")

puts "Tagged #{tag}"
puts ""
puts "To push the tag, run:"
puts "  git push --tags"
